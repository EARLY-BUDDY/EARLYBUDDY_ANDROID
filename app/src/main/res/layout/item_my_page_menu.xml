<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item_my_cl_back"
        android:layout_width="match_parent"
        android:layout_height="60dp">

        <TextView
            android:id="@+id/item_my_tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:fontFamily="@font/noto_sans_kr_regular"
            android:letterSpacing="-0.05"
            android:lineSpacingExtra="-4sp"
            android:textColor="@color/black"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tool:text="@string/item_my_page_title" />

        <ImageView
            android:id="@+id/item_my_iv_detail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="11dp"
            android:src="@drawable/ic_detail_light"
            app:layout_constraintBottom_toBottomOf="@id/item_my_tv_title"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/item_my_tv_title" />

        <View
            android:id="@+id/item_my_line"
            android:layout_width="0dp"
            android:layout_height="0.2dp"
            android:layout_marginTop="18dp"
            android:background="@color/light_gray"
            app:layout_constraintEnd_toEndOf="@id/item_my_iv_detail"
            app:layout_constraintStart_toStartOf="@id/item_my_tv_title"
            app:layout_constraintTop_toBottomOf="@+id/item_my_tv_title" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>